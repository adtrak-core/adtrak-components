<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{% block title %}Adtrak Modular Web Design{% endblock %}</title>
    <meta charset="UTF-8"/>
    {% if description %}
      <meta name="description" content="{{description}}"/>
    {% endif %}
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>
    <link rel="stylesheet" href="/style.css?v={% version %}"/>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    {% block head %}{% endblock %}
  </head>
  <body class="bg-gray-200">
    {% if component %}
      <div class="container pt-8">
        <div class="flex flex-wrap items-start">
          <div class="w-auto">
            <a class="text-blue hover:text-green focus:text-green underline text-sm" href="/">← Back to all components</a>
            <h1 class="block text-3xl font-bold mb-4 md:mb-6 leading-snug">{{ component.name }} Components</h1>
          </div>
          <div class="ml-auto hidden md:block">
            <span class="block text-sm">Last Updated - <span class="font-bold">{{ page.date | readableDate }}</span></span>
          </div>
        </div>
      </div>
    {% endif %}

    {% block content %}
      {{ content | safe }}
    {% endblock %}
  </body>
</html>