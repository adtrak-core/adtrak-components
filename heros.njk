---
date: Last Modified
component:
  name: Heros
---

{% extends "_includes/default.njk" %}

{% block content %}
  <div class="container">
    {%- for post in collections.heros -%}
      <section class="mb-12 md:mb-16">
        <span class="block font-mono mb-2">{{ component.name }} - Variant {{ loop.index }}</span>
        {% include "components/heros/hero-0"+ (loop.index) + ".njk" %}
      </section>
    {% endfor %}
  </div>
{% endblock %}